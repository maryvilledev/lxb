machine:
  environment:
    GO15VENDOREXPERIMENT: 1

dependencies:
  override:
    - go get github.com/Masterminds/glide
    - glide up

test:
  override:
    - go test
    - go build -o $CIRCLE_ARTIFACTS/lxb